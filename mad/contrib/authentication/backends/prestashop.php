<?php

class madAuthenticationPrestashopBackend implements madAuthenticationBackend {
    /**
     * Checks wether a username and password match and return the user object. 
     * False otherwise.
     * 
     * @param string $username 
     * @param string $password 
     * @return false|object False if fails.
     */
    public function authenticate( $username, $password ) {

    }
}

?>
