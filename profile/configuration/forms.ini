[profile.profile]
label=enregistrement de profil
fields[namespace][value]=profile

fields[name][label]=nom du profil
fields[name][required]=true

fields[introduction][label]=texte d'introduction
fields[introduction][widget][class]=textarea
fields[introduction][required]=true

fields[presentation][label]=texte de presentation
fields[presentation][widget][class]=textarea

fields[picture][label]=photo
fields[picture][widget][class]=file
fields[picture][required]=true

fields[email][label]=email
fields[email][required]=true

multipleFields[sites][label]=sites internets
multipleFields[sites][widget][class]=multiple values

multipleFields[products][label]=produits favoris
multipleFields[products][widget][class]=multiple values
